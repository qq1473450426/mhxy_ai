services:
  redis:
    image: redis:7-alpine
    container_name: mhxy-ai-redis
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - mhxy_redis_data:/data

volumes:
  mhxy_redis_data:
